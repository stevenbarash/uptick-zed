id = "versionlens"
name = "VersionLens"
version = "0.1.0"
schema_version = 1
description = "Shows the latest version of each package inline in package.json, Cargo.toml, pubspec.yaml, and composer.json. Port of the VSCode VersionLens extension."
authors = ["Steven Barash"]
repository = "https://github.com/stevenbarash/versionlens-zed"

[language_servers.versionlens]
name = "VersionLens"
languages = ["JSON", "JSONC", "TOML", "YAML"]
